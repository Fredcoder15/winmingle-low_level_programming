#include "main.h"

/* Author: Akpan-fred, Promise Friday
 * Program: WinMingle Community C Training
 * Description: This program prints every minute in every hour of the day as a digital clock. the main file calls the function but with empty parameter because the function takes no parameter and returns none
  */


int  main(void){
      jack_bauer();
	  return(0);
}
