#include "main.h"
#include <stdio.h>

int main(void){
int len;

char *str = "Thank you Jesus, I am grateful!";

len = _strlen(str);

printf("The length of my sentence is :%d\n", len);


print_rev(str);

print_rev(str);

return(0);
}
